channels:
  - defaults
  - conda-forge

dependencies:
  - pip
  - ipython
  - jupyter
  - numpy
  - numpydoc
  - sphinx>=1.7.5
  - nbsphinx
  - pandoc
  - astropy  # need this here, because we base classes off it (and docs need that).
  - pyyaml
  - matplotlib
  - pip:
    - sphinx-rtd-theme
    - setuptools_scm
